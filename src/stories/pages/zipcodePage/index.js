import DaumPostcode from "react-daum-postcode";

const ZipCodePage = () => {
  return (
    <div>
      <DaumPostcode
      // style={postCodeStyle}
      // onComplete={onCompletePost}
      ></DaumPostcode>
    </div>
  );
};

export default ZipCodePage;
